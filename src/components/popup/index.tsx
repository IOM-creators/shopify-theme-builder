import { h, render } from "preact";
import { Popup } from "./Popup";

export default (element, data) => {
  return <Popup />;
  // const init = () => {
  //   render(<Popup />, element);
  // };
  // init();
};
